[props]
pic = LOC_ICO/buttons/home.gif
url = ?reset=1&vmode=view

[de]
caption =
tooltip = Reset: setzt alle Sitzungs-Variablen auf 0.

[en]
caption =
tooltip = Reset: will set all session variables to 0.
